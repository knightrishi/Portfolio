function HeroSection() {
  return
(   
   <section id="hero" className="min-h-screen flex flex-col items-center justify-center">
    <h1 className="text-6xl font-serif">Arnav Singh</h1>
    <p className="text-lg mt-">Building logic that feels human.</p>
    <button className="mt-6 bg-sienna text-red px-6 py-2 rounded-lg hover:bg-mustard hover:text-choco transition">See my work</button>
   </section>
  );
}

export default HeroSection;